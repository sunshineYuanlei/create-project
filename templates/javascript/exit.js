const process = require('process');
let a = 0;
  
// Infinite loop
while (a === 0) {
  
    // Printing statement
    console.log('GeeksForGeeks');
      
    // Terminate the entire process
    process.exit(6);
}